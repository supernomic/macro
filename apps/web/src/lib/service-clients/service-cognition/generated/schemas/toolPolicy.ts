/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Document Cognition Service
 * OpenAPI spec version: 1.0.0
 */

import type { PolicyDecision } from './policyDecision';
import type { ToolPolicyApproverTeamId } from './toolPolicyApproverTeamId';
import type { ToolPolicyApproverUserId } from './toolPolicyApproverUserId';
import type { ToolPolicyOrgId } from './toolPolicyOrgId';

/**
 * An org-configurable policy row: which decision applies to an agent +
tool pair, and who approves when the decision is `require_approval`.
 */
export interface ToolPolicy {
  /** Agent slug pattern (`techops`, `*`, `tech*`). */
  agent_slug: string;
  /** Approver team queue, when `require_approval`. */
  approver_team_id?: ToolPolicyApproverTeamId;
  /** Direct approver, when `require_approval`. */
  approver_user_id?: ToolPolicyApproverUserId;
  /** The decision this policy imposes. */
  decision: PolicyDecision;
  /** Policy id. */
  id: string;
  /** Owning organization. */
  org_id?: ToolPolicyOrgId;
  /** Tool name pattern (`send_email`, `*`, `email.*`). */
  tool_name: string;
}
