/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Document Cognition Service
 * OpenAPI spec version: 1.0.0
 */
import type { EscalationAssigneeTeamId } from './escalationAssigneeTeamId';
import type { EscalationAssigneeUserId } from './escalationAssigneeUserId';
import type { EscalationCallbackUrl } from './escalationCallbackUrl';
import type { EscalationClaimedAt } from './escalationClaimedAt';
import type { EscalationOrgId } from './escalationOrgId';
import type { EscalationRequesterUserId } from './escalationRequesterUserId';
import type { EscalationResolution } from './escalationResolution';
import type { EscalationResolvedAt } from './escalationResolvedAt';
import type { EscalationResolvedBy } from './escalationResolvedBy';
import type { EscalationSessionId } from './escalationSessionId';
import type { EscalationSourceChannel } from './escalationSourceChannel';
import type { EscalationStatus } from './escalationStatus';
import type { Priority } from './priority';

/**
 * One escalation: an agent's request for human-expert input.
 */
export interface Escalation {
  /** Team whose queue holds it (set on team routing). */
  assignee_team_id?: EscalationAssigneeTeamId;
  /** Person who owns it (set on claim / direct routing). */
  assignee_user_id?: EscalationAssigneeUserId;
  /** URL the runtime is called back on when the escalation resolves. */
  callback_url?: EscalationCallbackUrl;
  /** Claim instant, when claimed. */
  claimed_at?: EscalationClaimedAt;
  /** Creation instant. */
  created_at: string;
  /** Domain the escalation belongs to (e.g. `techops`). */
  domain: string;
  /** Escalation id (UUID v7). */
  id: string;
  /** Owning organization, when the creating principal is org-scoped. */
  org_id?: EscalationOrgId;
  /** Urgency. */
  priority: Priority;
  /** Human-readable requester (e.g. Slack handle) for display. */
  requester_display: string;
  /** Macro user id of the person who originally asked, when known. */
  requester_user_id?: EscalationRequesterUserId;
  /** The expert's answer (set on resolve). */
  resolution?: EscalationResolution;
  /** Resolution instant, when resolved or cancelled. */
  resolved_at?: EscalationResolvedAt;
  /** Macro user id of the resolver. */
  resolved_by?: EscalationResolvedBy;
  /** Ledger session of the conversation that escalated, when any. */
  session_id?: EscalationSessionId;
  /** Source channel of the originating conversation (e.g. `slack`). */
  source_channel?: EscalationSourceChannel;
  /** Lifecycle status. */
  status: EscalationStatus;
  /** What the agent tried and where it got stuck (the expert's briefing). */
  summary: string;
  /** Use-case tags for routing (e.g. `vpn`, `sso`). */
  tags: string[];
  /** Short title for inbox cards. */
  title: string;
}
