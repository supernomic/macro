/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Document Cognition Service
 * OpenAPI spec version: 1.0.0
 */

import type { OkfStatus } from './okfStatus';
import type { SkillRecordArchivedAt } from './skillRecordArchivedAt';
import type { SkillRecordOrgId } from './skillRecordOrgId';
import type { SkillRecordOwnerTeamId } from './skillRecordOwnerTeamId';
import type { SkillRecordOwnerUserId } from './skillRecordOwnerUserId';
import type { SkillRecordStaleAfter } from './skillRecordStaleAfter';
import type { SkillScope } from './skillScope';
import type { TrustTier } from './trustTier';

/**
 * A governed skill record.
 */
export interface SkillRecord {
  /** Soft-delete timestamp. */
  archived_at?: SkillRecordArchivedAt;
  /** Full SKILL.md body (instructions). */
  body: string;
  /** SHA-256 hex of `body`. */
  content_hash: string;
  /** Created at. */
  created_at: string;
  /** One-or-two sentence description (the catalog line the model always sees). */
  description: string;
  /** Skill id. */
  id: string;
  /** Display name. */
  name: string;
  /** Whether an agent generated this content. */
  okf_generated: boolean;
  /** Source URIs or ids. */
  okf_sources: string[];
  /** Lifecycle status. */
  okf_status: OkfStatus;
  /** OKF type (`skill`). */
  okf_type: string;
  /** Whether a human has verified the content. */
  okf_verified: boolean;
  /** Owning organization; `None` for platform skills. */
  org_id?: SkillRecordOrgId;
  /** Owner team when `scope` is `team`. */
  owner_team_id?: SkillRecordOwnerTeamId;
  /** Owner user when `scope` is `user`. */
  owner_user_id?: SkillRecordOwnerUserId;
  /** Ownership scope. */
  scope: SkillScope;
  /** Stable slug (directory name / Flue skill name). */
  slug: string;
  /** When the content should be considered stale. */
  stale_after?: SkillRecordStaleAfter;
  /** Trust tier. */
  trust_tier: TrustTier;
  /** Last updated at. */
  updated_at: string;
  /** Monotonic version; each approve/rollback bumps this. */
  version: number;
}
