/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Document Cognition Service
 * OpenAPI spec version: 1.0.0
 */
import type { ApprovalRequestAssigneeTeamId } from './approvalRequestAssigneeTeamId';
import type { ApprovalRequestAssigneeUserId } from './approvalRequestAssigneeUserId';
import type { ApprovalRequestCallbackUrl } from './approvalRequestCallbackUrl';
import type { ApprovalRequestConsumedAt } from './approvalRequestConsumedAt';
import type { ApprovalRequestDecidedAt } from './approvalRequestDecidedAt';
import type { ApprovalRequestDecidedBy } from './approvalRequestDecidedBy';
import type { ApprovalRequestDecisionNote } from './approvalRequestDecisionNote';
import type { ApprovalRequestOrgId } from './approvalRequestOrgId';
import type { ApprovalRequestRequesterUserId } from './approvalRequestRequesterUserId';
import type { ApprovalRequestSessionId } from './approvalRequestSessionId';
import type { ApprovalStatus } from './approvalStatus';

/**
 * One approval request: a gated tool call waiting for (or decided by) a
human.
 */
export interface ApprovalRequest {
  /** Slug of the agent principal that hit the gate. */
  agent_slug: string;
  /** The proposed arguments, shown verbatim to the approver. */
  arguments: unknown;
  /** SHA-256 hex digest of the canonical arguments. */
  arguments_digest: string;
  /** Approver team queue, when routed to a team. */
  assignee_team_id?: ApprovalRequestAssigneeTeamId;
  /** Direct approver, when routed to a person. */
  assignee_user_id?: ApprovalRequestAssigneeUserId;
  /** URL the runtime is called back on when decided. */
  callback_url?: ApprovalRequestCallbackUrl;
  /** When the decided request was consumed by a retry of the call. */
  consumed_at?: ApprovalRequestConsumedAt;
  /** Creation instant. */
  created_at: string;
  /** Decision instant. */
  decided_at?: ApprovalRequestDecidedAt;
  /** Macro user id of the decider. */
  decided_by?: ApprovalRequestDecidedBy;
  /** Optional note from the decider (shown to the agent and requester). */
  decision_note?: ApprovalRequestDecisionNote;
  /** Request id (UUID v7). */
  id: string;
  /** Owning organization. */
  org_id?: ApprovalRequestOrgId;
  /** Human-readable requester for inbox cards. */
  requester_display: string;
  /** Macro user id of the person the agent is acting for, when known. */
  requester_user_id?: ApprovalRequestRequesterUserId;
  /** Ledger session of the gated conversation. */
  session_id?: ApprovalRequestSessionId;
  /** Lifecycle status. */
  status: ApprovalStatus;
  /** The agent's explanation of what it wants to do and why. */
  summary: string;
  /** The gated tool. */
  tool_name: string;
}
