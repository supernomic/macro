/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Document Cognition Service
 * OpenAPI spec version: 1.0.0
 */

import type { MirrorProvider } from './mirrorProvider';
import type { TicketMirrorDisconnectedAt } from './ticketMirrorDisconnectedAt';
import type { TicketMirrorForeignUrl } from './ticketMirrorForeignUrl';
import type { TicketMirrorOrgId } from './ticketMirrorOrgId';

/**
 * A live (or disconnected) mirror of a Macro entity onto an external ticket.
 */
export interface TicketMirror {
  /** When disconnected; native entity is unchanged. */
  disconnected_at?: TicketMirrorDisconnectedAt;
  /** External ticket id. */
  foreign_id: string;
  /** External ticket URL (backlink). */
  foreign_url?: TicketMirrorForeignUrl;
  /** Mirror id. */
  id: string;
  /** Last successful mirror. */
  last_mirrored_at: string;
  /** Macro entity id. */
  native_entity_id: string;
  /** Macro entity type (source of truth). */
  native_entity_type: string;
  /** Owning organization. */
  org_id?: TicketMirrorOrgId;
  /** Provider. */
  provider: MirrorProvider;
  /** External status snapshot. */
  status: string;
  /** Summary mirrored outward. */
  summary: string;
}
