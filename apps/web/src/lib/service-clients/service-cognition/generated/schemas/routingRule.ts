/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Document Cognition Service
 * OpenAPI spec version: 1.0.0
 */

import type { RouteTarget } from './routeTarget';
import type { RoutingRuleMinPriority } from './routingRuleMinPriority';
import type { RoutingRuleOrgId } from './routingRuleOrgId';
import type { RoutingRuleSourceChannel } from './routingRuleSourceChannel';

/**
 * A routing rule: conditions → target, evaluated in `position` order; the
first matching rule wins.
 */
export interface RoutingRule {
  /** Domain this rule applies to. */
  domain: string;
  /** Rule id. */
  id: string;
  min_priority?: RoutingRuleMinPriority;
  /** Owning organization. */
  org_id?: RoutingRuleOrgId;
  /** Evaluation order (ascending; first match wins). */
  position: number;
  /** Source-channel condition: matches when absent or equal. */
  source_channel?: RoutingRuleSourceChannel;
  /** Tags condition: matches when empty or intersecting the escalation's
tags. */
  tags: string[];
  /** Where matching escalations go. */
  target: RouteTarget;
}
