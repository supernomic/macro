/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Document Cognition Service
 * OpenAPI spec version: 1.0.0
 */
import type { ApprovalTransitionFromTeamId } from './approvalTransitionFromTeamId';
import type { ApprovalTransitionFromUserId } from './approvalTransitionFromUserId';
import type { ApprovalTransitionReason } from './approvalTransitionReason';
import type { ApprovalTransitionToTeamId } from './approvalTransitionToTeamId';
import type { ApprovalTransitionToUserId } from './approvalTransitionToUserId';

/**
 * An audited state transition on an approval request.
 */
export interface ApprovalTransition {
  /** Action name (`routed`, `approved`, `denied`, `reassigned`,
`cancelled`, `consumed`). */
  action: string;
  /** Who performed it (Macro user id, agent principal id, or `system`). */
  actor_id: string;
  /** The approval request. */
  approval_id: string;
  /** Instant of the transition. */
  created_at: string;
  /** Assignee team before the transition. */
  from_team_id?: ApprovalTransitionFromTeamId;
  /** Assignee user before the transition. */
  from_user_id?: ApprovalTransitionFromUserId;
  /** Transition id. */
  id: string;
  /** Free-form reason (required for reassign). */
  reason?: ApprovalTransitionReason;
  /** Assignee team after the transition. */
  to_team_id?: ApprovalTransitionToTeamId;
  /** Assignee user after the transition. */
  to_user_id?: ApprovalTransitionToUserId;
}
