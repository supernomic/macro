/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Document Cognition Service
 * OpenAPI spec version: 1.0.0
 */
import type { EscalationTransitionFromTeamId } from './escalationTransitionFromTeamId';
import type { EscalationTransitionFromUserId } from './escalationTransitionFromUserId';
import type { EscalationTransitionReason } from './escalationTransitionReason';
import type { EscalationTransitionToTeamId } from './escalationTransitionToTeamId';
import type { EscalationTransitionToUserId } from './escalationTransitionToUserId';

/**
 * An audited state transition on an escalation (claim, reassign, resolve).
 */
export interface EscalationTransition {
  /** Action name (`routed`, `claimed`, `reassigned`, `resolved`,
`cancelled`). */
  action: string;
  /** Who performed it (Macro user id, agent principal id, or `system`). */
  actor_id: string;
  /** Instant of the transition. */
  created_at: string;
  /** The escalation. */
  escalation_id: string;
  /** Assignee team before the transition. */
  from_team_id?: EscalationTransitionFromTeamId;
  /** Assignee user before the transition. */
  from_user_id?: EscalationTransitionFromUserId;
  /** Transition id. */
  id: string;
  /** Free-form reason (required for reassign; feeds rule refinement). */
  reason?: EscalationTransitionReason;
  /** Assignee team after the transition. */
  to_team_id?: EscalationTransitionToTeamId;
  /** Assignee user after the transition. */
  to_user_id?: EscalationTransitionToUserId;
}
