/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Document Cognition Service
 * OpenAPI spec version: 1.0.0
 */

import type { ProposalKind } from './proposalKind';
import type { ProposalStatus } from './proposalStatus';
import type { SkillProposalAssigneeTeamId } from './skillProposalAssigneeTeamId';
import type { SkillProposalAssigneeUserId } from './skillProposalAssigneeUserId';
import type { SkillProposalDecidedAt } from './skillProposalDecidedAt';
import type { SkillProposalDecidedBy } from './skillProposalDecidedBy';
import type { SkillProposalDecisionNote } from './skillProposalDecisionNote';
import type { SkillProposalEvalPassed } from './skillProposalEvalPassed';
import type { SkillProposalEvalRunId } from './skillProposalEvalRunId';
import type { SkillProposalOrgId } from './skillProposalOrgId';
import type { SkillProposalOwnerTeamId } from './skillProposalOwnerTeamId';
import type { SkillProposalOwnerUserId } from './skillProposalOwnerUserId';
import type { SkillProposalProposerAgentId } from './skillProposalProposerAgentId';
import type { SkillProposalProposerUserId } from './skillProposalProposerUserId';
import type { SkillProposalSkillId } from './skillProposalSkillId';
import type { SkillProposalSnapshotId } from './skillProposalSnapshotId';
import type { SkillScope } from './skillScope';

/**
 * A staged skill write awaiting (or having received) inbox review.
 */
export interface SkillProposal {
  /** Team queue. */
  assignee_team_id?: SkillProposalAssigneeTeamId;
  /** Direct assignee. */
  assignee_user_id?: SkillProposalAssigneeUserId;
  /** Created at. */
  created_at: string;
  /** When decided. */
  decided_at?: SkillProposalDecidedAt;
  /** Who decided. */
  decided_by?: SkillProposalDecidedBy;
  /** Decision note. */
  decision_note?: SkillProposalDecisionNote;
  /** Human-readable diff summary (evidence-backed, prime-agent `/refine` shape). */
  diff_summary: string;
  /** Whether that eval passed. */
  eval_passed?: SkillProposalEvalPassed;
  /** Eval run that gated promotion, when any. */
  eval_run_id?: SkillProposalEvalRunId;
  /** Trace excerpts / eval pointers backing the change. */
  evidence: unknown;
  /** Proposal id. */
  id: string;
  /** Create / patch / archive. */
  kind: ProposalKind;
  /** Owning organization. */
  org_id?: SkillProposalOrgId;
  /** Owner team when targeting team scope. */
  owner_team_id?: SkillProposalOwnerTeamId;
  /** Owner user when targeting user scope. */
  owner_user_id?: SkillProposalOwnerUserId;
  /** Proposed body. */
  proposed_body: string;
  /** Proposed catalog description. */
  proposed_description: string;
  /** Proposed name. */
  proposed_name: string;
  /** Proposing agent principal id, when an agent authored this. */
  proposer_agent_id?: SkillProposalProposerAgentId;
  /** Proposing user id, when a human authored this. */
  proposer_user_id?: SkillProposalProposerUserId;
  /** Existing skill, when patching or archiving. */
  skill_id?: SkillProposalSkillId;
  /** Target slug. */
  slug: string;
  /** Snapshot taken at approve time (for rollback). */
  snapshot_id?: SkillProposalSnapshotId;
  /** Review status. */
  status: ProposalStatus;
  /** Target scope. */
  target_scope: SkillScope;
  /** Last updated at. */
  updated_at: string;
}
