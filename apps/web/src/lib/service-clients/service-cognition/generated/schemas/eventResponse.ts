/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Document Cognition Service
 * OpenAPI spec version: 1.0.0
 */
import type { EventResponseOrgId } from './eventResponseOrgId';
import type { EventResponsePayload } from './eventResponsePayload';

/**
 * A stored ledger event.
 */
export interface EventResponse {
  /** Actor id. */
  actor_id: string;
  /** Actor kind. */
  actor_kind: string;
  /** Stored event type discriminant. */
  event_type: string;
  /** Chain hash (hex). */
  hash: string;
  /** When the event occurred. */
  occurred_at: string;
  /** Organization scope. */
  org_id?: EventResponseOrgId;
  /** The typed payload, adjacently tagged. */
  payload: EventResponsePayload;
  /** Position in the session. */
  seq: number;
  /** Session id. */
  session_id: string;
  /** Provenance links. */
  source_event_seqs: number[];
}
